diff --git a/src/App.css b/src/App.css
index 1b6fe02..261b879 100644
--- a/src/App.css
+++ b/src/App.css
@@ -105,29 +105,16 @@
 #yt-player,
 .yt-player-hidden {
   position: fixed !important;
-  width: 320px !important;
-  height: 180px !important;
-  left: -10000px !important;
-  top: -10000px !important;
-  opacity: 0 !important;
-  pointer-events: none !important;
-  border: 0 !important;
-  z-index: -1 !important;
-}
-
-.yt-player-hidden {
-  position: fixed !important;
-  width: 320px !important;
-  height: 180px !important;
-
-  left: -1000px !important;
-  top: -1000px !important;
-
+  bottom: 0 !important;
+  right: 0 !important;
+  width: 200px !important;
+  height: 200px !important;
+  overflow: hidden !important;
+  clip: rect(0, 0, 0, 0) !important;
+  clip-path: inset(50%) !important;
   opacity: 0.01 !important;
   pointer-events: none !important;
-
   border: 0 !important;
-  overflow: hidden !important;
   z-index: -1 !important;
 }
 
diff --git a/src/hooks/useYouTubePlayer.ts b/src/hooks/useYouTubePlayer.ts
index f477386..7506091 100644
--- a/src/hooks/useYouTubePlayer.ts
+++ b/src/hooks/useYouTubePlayer.ts
@@ -43,7 +43,7 @@ export function useYouTubePlayer(containerId: string) {
 
   const stopTick = useCallback(() => {
     if (tickRef.current !== null) {
-      cancelAnimationFrame(tickRef.current);
+      clearInterval(tickRef.current);
       tickRef.current = null;
     }
   }, []);
@@ -59,9 +59,9 @@ export function useYouTubePlayer(containerId: string) {
           duration: player.getDuration() || prev.duration,
         }));
       }
-      tickRef.current = requestAnimationFrame(tick);
     };
-    tickRef.current = requestAnimationFrame(tick);
+    tick();
+    tickRef.current = window.setInterval(tick, 500);
   }, [stopTick]);
 
   const loadPlaylistMeta = useCallback(async (videoIds: string[]) => {
@@ -100,8 +100,8 @@ export function useYouTubePlayer(containerId: string) {
 
         await new Promise<void>((resolve, reject) => {
           playerRef.current = new window.YT.Player(containerId, {
-            height: "180",
-            width: "320",
+            height: "200",
+            width: "200",
             playerVars:
               isMix && seedVideoId
                 ? {
